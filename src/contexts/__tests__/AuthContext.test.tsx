import { describe, it } from 'vitest'

// Skip Auth0 tests since we're now using Firebase authentication primarily
describe('AuthContext (Auth0 - Legacy)', () => {
  it.skip('Auth0 tests skipped - now using Firebase authentication', () => {
    // These tests were for the legacy Auth0 authentication system
    // They are skipped since we now prioritize Firebase authentication
  })
})
